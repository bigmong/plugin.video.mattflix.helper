v2.5.0 (2026-08-26)
- BREAKING -- widget URLs have changed. Lists are now addressed as ?list=<name> and
  actions as ?mode=<name>. The old ?key=<name> has been REMOVED rather than kept as an
  alias, so any widget pointed at plugin://plugin.video.mattflix.helper/?key=... must be
  repointed to ?list=... . A stale binding falls back to showing the addon's root menu
  and logs "No ?list= or ?mode= in query ...", which is the quickest way to spot one.
- Added "Want to Watch Movies" and "Want to Watch Shows" widgets, backed by your Jellyfin
  favourites. Heart something in any Jellyfin client and it turns up in the widget on
  every box. Needs no API key and no per-box setup: it reuses the login the Jellyfin
  addon has already stored, so each box shows the list for whichever user it is signed
  in as. Items are ordered by date added, newest first. Nothing is removed from the list
  by the addon -- watched items stay until something clears the favourite server-side,
  which is not in place yet.
- Fixed: watched status now shows on widget items. Previously every item rendered as
  unwatched no matter what the library said, because these widgets build their list
  items from scratch and playcount was never carried across. Movies now carry playcount,
  last-played and resume points, so part-watched films get their progress bar back, and
  shows carry watched/unwatched episode counts.
- Fixed: the watched-progress pie now appears on part-watched shows. It is driven by a
  separate WatchedEpisodePercent property that was never set, so the episode counts
  rendered while the pie silently did not.

v2.0.9 (2026-08-16)
- Version bump only; no functional changes.

v2.0.8 (2026-08-15)
- List URLs now accept ?limit=N, capping the number of matched results. The full list
  is still fetched, so the cap bounds the top-N local matches rather than truncating
  the source fetch.
- Tidied up list rendering.

v2.0.7 (2026-08-14)
- Added seasonal, date-windowed lists (Christmas, Halloween, Valentine's Day,
  May the 4th, Star Trek Day, Nick November, Friday the 13th). These return empty
  outside their window so the skin's auto-hide takes care of showing/hiding them.
- Added a debug logging setting.

v2.0.6 (2026-08-14)
- Corrected the addon ID to plugin.video.mattflix.helper.
  NOTE: this changed the addon's identity -- installs on the previous ID
  (plugin.video.mattflixhelper) do not upgrade to this and need reinstalling.

v2.0.5 (2026-08-14)
- Added an en_GB language file; settings labels are now translatable strings
  rather than hardcoded text.

v2.0.4 (2026-08-14)
- Custom list settings accept empty values, so a slot can be left blank without
  breaking the list.

v2.0.3 (2026-08-14)
- TV show matching now also indexes TVDB IDs, improving hit rate against the
  local library.
- Custom list settings gained descriptive headings.
- Added diagnostic logging for show titles that fail to match anything locally.

v2.0.2 (2026-08-14)
- Custom lists: user-defined slots configurable from the addon's own settings
  (label, type, URL, optional window) with no redeploy needed.
- Fixed the trending movies and trending shows list URLs.

v2.0.1 (2026-08-14)
- Repository and build plumbing; corrected addon metadata URLs.
- Removed an obsolete build script.

v2.0.0 (2026-08-13)
- Initial release. Curated movie/show widgets for Arctic Fuse, resolved live
  against the local Kodi library via JSON-RPC, sourced from MDBList or the
  local library.
